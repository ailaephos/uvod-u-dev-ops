apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: api-configmap-reader-binding
subjects:
  - kind: ServiceAccount
    name: api-sa
    namespace: default
roleRef:
  kind: Role
  name: api-configmap-reader
  apiGroup: rbac.authorization.k8s.io